#!/usr/bin/env bash
set -euo pipefail
# Auto-fill a minimal commit message if none was provided.
msg_file="$1"
# If message already has content, leave it.
if [[ -s "$msg_file" ]]; then
  exit 0
fi
# For empty message (e.g., VS Code commit box left blank), set a default.
echo "z" >"$msg_file"
